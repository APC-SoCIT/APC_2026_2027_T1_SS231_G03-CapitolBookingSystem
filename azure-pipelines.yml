## Node.js with React
# Build a Node.js project that uses React.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- main

variables:
  buildConfiguration: 'production'

stages:
- stage: BuildAndDeploy
  displayName: Build and Deploy to Staging
  jobs:
  - deployment: DeployToStaging
    displayName: Deploy to Staging VM
    environment:
      name: Staging
      resourceType: VirtualMachine

    strategy:
      runOnce:
        deploy:
          steps:
          # 1. Fetch repository code
          - checkout: self

          # 2. Build the frontend on the VM
          - task: NodeTool@0
            inputs:
              versionSpec: '20.x'
            displayName: 'Install Node.js'

          - script: |
              npm install
            displayName: 'npm install'

          - script: |
              npm run build
            env:
              VITE_SUPABASE_URL: $(VITE_SUPABASE_URL)
              VITE_SUPABASE_PUBLISHABLE_KEY: $(VITE_SUPABASE_PUBLISHABLE_KEY)
            displayName: 'npm run build'

          # 3. Stop IIS
          - powershell: |
              Import-Module WebAdministration
              Stop-WebSite "Default Web Site"
            displayName: Stop IIS

          # 4. Copy dist files to wwwroot
          - powershell: |
              $sitePath = "C:\inetpub\wwwroot"

              if (!(Test-Path $sitePath)) {
                  New-Item -ItemType Directory -Path $sitePath -Force
              }

              Remove-Item "$sitePath\*" -Recurse -Force -ErrorAction SilentlyContinue

              Copy-Item "$(System.DefaultWorkingDirectory)\dist\*" `
                        "$sitePath" `
                        -Recurse `
                        -Force

              Write-Host "Deployment completed."
            displayName: Copy Files

          # 5. Start IIS
          - powershell: |
              Import-Module WebAdministration
              Start-WebSite "Default Web Site"
            displayName: Start IIS
